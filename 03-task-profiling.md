# 训练经验 Memory 独立研究集（v6：独立化架构 + 分层画像模型）

> 分支：`work/20260810-training-experience-memory-research` ｜ 日期：2026-08-10 ｜ 状态：设计讨论稿
> **定位变更**：Memory 是**独立于 ATF 的通用训练经验系统**，ATF 只是第一个消费者。本文重构为独立架构 + 分层画像模型。

## 0. 定位：从"ATF 的模块"改为"独立系统 + 消费方接入"

用户明确要求：memory **不是强绑定 ATF 的**，但 ATF 也要能用，且可以拿出来独立讨论研究。

设计原则：
1. **核心层与消费方解耦**：经验卡 schema、画像、检索、回灌、存储全部用**通用训练术语**定义，不依赖任何 ATF 概念（无 "ArtifactRef"、"Gate"、"envelope"）。
2. **接入层适配**：每个消费方（ATF、未来 CLI/Web/其他工具）通过适配接口接入；ATF 的 Artifact/Decision/Approval 概念映射到 memory 的通用接口（见 §5）。
3. **独立存储**：memory 数据（经验卡/画像/索引）存于独立存储（`memory.db`/目录），不寄生在消费方事实库里。
4. **独立研究集**：文档自成体系（本目录），可独立阅读、独立演进、独立发布。

```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  消费方 1    │   │  消费方 2    │   │   未来消费方  │
│  (ATF)      │   │  (CLI/Web)  │   │  (其他工具)   │
└──────┬──────┘   └──────┬──────┘   └──────┬──────┘
       │                 │                 │
       └─────────────────┼─────────────────┘
                         ▼
              ┌──────────────────────┐
              │   Memory 接入层       │  ← 适配接口：输入任务素材/输出画像/输出建议/接收验证结果
              └──────────────────────┘
                         ▼
              ┌──────────────────────┐
              │   Memory 核心层       │
              │  · 分层画像引擎        │
              │  · 字段语义匹配        │
              │  · 检索与推荐          │
              │  · 经验回灌与状态机     │
              └──────────────────────┘
                         ▼
              ┌──────────────────────┐
              │  独立存储             │  ← memory.db：经验卡/画像/索引（不寄生消费方）
              └──────────────────────┘
```

## 1. 分层画像模型（v6 核心重构）

用户指出：8 方向平铺不够。画像应是**分层的**，且**字段层是核心**——因为不同单据字段不同，对 memory 中没有的新单据，必须从字段语义入手找相似经验。

```
L1 单据类别层   单据类型/业务域/任务形态（lane、是否 bbox、是否跨页）
   ↓ 类别识别
L2 版式层      版式标签、布局结构、跨单据类型版式相似性（联合比对）
   ↓ 版式比对
L3 字段层（核心）字段语义匹配（别名/语义相似）+ 字段值形态（单值/多值/归组）+ 字段分布
   ↓ 字段映射
L4 数据工程层  样式类型分布统计、去重闭包、正负样本、空值语义、质量门禁
   ↓ 综合
任务画像（task-profile/v1，分层结构）+ 检索键
```

### L1 单据类别层
| 项 | 说明 |
|---|---|
| 单据类型识别 | 新单据 vs 已知类别（memory 中是否有同类） |
| 业务域 | 银行/海关/物流/贸易… |
| 任务形态 | lane、bbox 需求、跨页、group 需求 |
| 产出 | `doc_type_profile` + "是否见过此类"标记 |

### L2 版式层（含跨类型联合比对）
| 项 | 说明 |
|---|---|
| 版式标签 | 密集表格/多栏/印章/手写/长表/旋转… |
| 布局结构特征 | 区域分布、行列结构、表头模式、页眉页脚位置 |
| **跨单据类型版式相似性** | 不同单据类型可能共享版式模式（如海运单与提货单都含 dense table）——计算**版式相似度矩阵**，把相似版式的历史经验纳入候选，**不限于同类单据** |
| **版式相似度算法（双路）** | ① 布局结构规则：区域/表格/行列/表头特征向量 + 结构距离（确定性、可解释）；② 视觉向量：页面缩略图 embedding（如 CLIP/DINOv2 类）余弦相似度；两路融合：结构规则为主（权重 0.6）+ 视觉向量（0.4），结果给出 Top-N 相似单据及其依据 |
| 产出 | `layout_profile` + 相似版式单据列表（含相似度与依据） |

### L3 字段层（新单据分析的核心）
对 memory 中没有的新单据，**优先从字段层面分析**：

**(a) 字段语义匹配（最核心）**——从已有样本的解析字段中查找相似语义字段（字段名不同但语义相近/相同）。**三路匹配全部纳入首轮实现**（含向量）：
```
新单据字段定义（字段名 + 字段说明 + 样本值样例）
   ↓
字段语义归一化（canonical field semantics），三路并行：
  ① 别名表/同义词表：product_no ↔ product number ↔ 产品编号；item_no 易混 product_no
  ② 值形态启发：字段样例值模式（编号/金额/数量/日期/名称）
  ③ 语义向量：字段名+说明+样例 的 embedding（如 bge-m3）余弦相似度
   ↓
融合打分：三路加权（别名命中=强证据权重最高，值形态=中，向量=低但覆盖面广）
   · 任一路命中高置信 → 直接映射；三路都不高 → 融合分排序 + 人工确认
   ↓
输出字段映射表：{新字段 → 已知语义字段, 匹配分数, 匹配依据[别名/值形态/向量], 各路明细}
   ↓
命中语义字段 → 迁移该字段的历史经验（负样本策略/长文本处理/易混对警示…）
未命中 → 标记"新字段"，走默认 SOP + 人工确认
```
- 匹配依据**可追溯**（命中哪条别名/哪个样例/哪条向量近邻），不是黑盒相似度；向量路保留 top-N 近邻作为依据。
- 输出同时给出**易混字段警示**（新字段与已知字段的向量近邻但语义不同，如 item_no/product_no）。
- 向量模型选择与阈值：bge-m3 或同规模中文 embedding；相似度阈值首轮定为 0.75（可调），低于阈值进人工。

**(b) 字段值形态**——同名字段值形态不同，经验不通用：
```
value_cardinality: single | multi | grouped   ← 货描归组字段 = grouped
  · single：一个字段一个值（如 invoice_no）
  · multi：一个字段多个值（如 shipping_marks 多行）
  · grouped：字段内归组成组（如 goods 明细：goods_name/goods_quantity/… 按行归组）
group_semantics: 归组键/组内字段/组间关系（行对齐、跨页组连续性）
```
- 货描的 group 字段（多值单值处理）就在这里刻画：组内字段集合、组数分布、行对齐机制（row_delta 异常是装箱单第一大失败模式）。
- **经验迁移必须值形态匹配**：grouped 字段的经验（长表分段/行对齐）不能迁移到 single 字段。

**(c) 字段分布**：support、值长度、单位/币种、易混对（沿用 v3）。

### L4 数据工程层（常规分析保持）
| 项 | 说明 |
|---|---|
| 样式类型分布统计 | 单据样式/模板分布（前 N 样式覆盖率、长尾样式） |
| 去重闭包 | raw SHA / pixel SHA / 感知 hash / family / cluster |
| 正负样本 | 真实空 vs 假空、负样本比例 |
| 质量门禁 | 缺图/解析失败/字段面四方对齐 |

## 2. 新单据（memory 无同类）的分析流程

```
新单据原始素材
  → L1：类别识别 → memory 中无同类 → 进入"新单据路径"
  → L2：版式比对 → 找相似版式单据（跨类型）→ 候选经验池 A
  → L3：字段语义匹配 → 逐个字段找相似语义字段 → 候选经验池 B（按字段）
  → L4：数据工程统计 → 常规质量画像
  → 综合画像（分层）→ 检索（字段映射命中 > 版式相似 > 类别）→ 建议卡
  → 人工确认：新字段语义映射是否正确（首轮必确认，之后复用）
```

**关键机制**：新单据不因"memory 没有同类单据"就无经验可用——**字段语义匹配让它能从"长得像的字段"迁移经验**（字段名不同但语义相同）。这比按单据类型检索跨得更远、更本质。

## 3. 检索键更新（分层优先级）

```
score = w1 × 字段语义映射命中率（L3，最高权重）
      + w2 × 版式相似度（L2，跨类型）
      + w3 × 单据类别命中（L1）
      + w4 × 值形态匹配（L3b，必须匹配否则过滤）
      + 时间衰减
```
- 值形态不匹配的经验**直接过滤**（grouped 经验不推荐给 single 字段）。
- 字段语义映射由画像的 `field_mapping` 提供（不是黑盒检索）。

## 4. Memory 核心模块（独立于 ATF 的通用设计）

| 模块 | 职责 | 通用接口 |
|---|---|---|
| `task-profilers` | 分层画像（L1-L4）+ 字段语义匹配 | in: 任务素材 refs / 字段定义 / 样本；out: `task-profile/v1` + `field_mapping` |
| `experience-retriever` | 分层检索 top-k | in: profile + 检索参数；out: 经验卡列表 + 评分 |
| `strategy-advisor` | 汇总建议卡 | in: top-k 卡；out: 建议（策略 diff + 证据 + 反证） |
| `experience-curator` | 回灌（candidate→验证→validated/rejected） | in: badcase 结论 + 验证结果；out: 经验卡 |
| `experience-store` | 独立存储 + 状态机 | CRUD + 检索索引 |
| `field-semantics` | 字段语义词典/匹配（别名表 + 值形态 + 向量） | 匹配查询/新增维护 |

## 5. ATF 接入方式（消费方适配，不改 memory 核心）

| ATF 概念 | Memory 通用概念 | 适配方式 |
|---|---|---|
| Artifact Catalog | 事实/证据引用 | ATF 侧把 ArtifactRef 映射为 memory 的 `evidence_ref`（字符串/URI） |
| Decision Ledger | 经验状态裁判记录 | ATF 的 Decision 记录映射为 memory 状态转换日志 |
| Suggestion envelope | 建议输出 | 适配层把 memory 建议转换为 Suggestion |
| Gate 体系 | 经验质量门禁 | ATF 的写入门禁在适配层执行（memory 核心只定义通用约束） |
| Skill（profile-task 等） | profilers/retriever 等 | ATF 的 Skill 是**薄包装**，核心逻辑在 memory 侧 |

**约束**：memory 核心**不 import 任何 ATF 代码**；ATF 的接入是单向依赖（ATF → memory 适配层 → memory 核心）。这样 memory 可独立发布/复用/测试，未来接 CLI/Web 零成本。

## 6. 研究集组织（独立可读）

```
TrainingExperienceMemory/           ← 独立研究集（本地 + 远程 docs/research/training-experience-memory/）
  README.md                          ← 研究索引与阅读路径
  01-vision-and-scope.md             ← v1：构想与四层模型
  02-research-and-precedents.md      ← v2：先例 + Qwen 历史经验
  03-task-profiling.md               ← v3+v6：任务画像（分层模型 L1-L4）
  04-architecture.md                 ← v4+v5：整体架构 + 模块设计
  05-consume-integration.md          ← ATF 接入 + 其他消费方
  06-experience-cards/               ← 首批经验卡（普适 + 自有）
```

## 7. 决策记录（2026-08-10 用户已拍板）

| # | 问题 | 决策 |
|---|---|---|
| 1 | 字段语义匹配实现路径 | **三路全部纳入首轮**：别名表 + 值形态启发 + 语义向量（bge-m3，阈值 0.75，可调）；融合打分，任一路高置信直接映射，否则人工确认 |
| 2 | 字段语义词典维护 | memory 自带 canonical field semantics 词典 + 消费方上报新字段、人工审核入库 |
| 3 | 版式相似度算法 | **双路纳入首轮**：布局结构规则（权重 0.6）+ 页面视觉向量 CLIP/DINOv2 类（权重 0.4）；输出 Top-N 相似单据及依据 |
| 4 | memory 独立程度 | 先独立目录 + 独立文档（本地 TrainingExperienceMemory/ + 远程 docs/research/training-experience-memory/），验证价值后再考虑独立 git 仓库 |

## 8. 下一步

1. 冻结 `experience-card/v1` + `task-profile/v1` schema（含 field_mapping、layout_similarity 结构）。
2. 字段语义词典初版（Qwen 已有字段 + 别名 + 值形态规则）。
3. 首批经验卡提炼（普适 10-15 + 自有 20-30）。
